[package]
name = "hyzero"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.9.2"
tokio = {version = "1", features = ["full"]}
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
bincode = "1"
